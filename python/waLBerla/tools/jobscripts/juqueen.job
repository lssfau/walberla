# @ job_name = {job_name}
# @ comment = "{job_name}"
# @ error = {error_file}.$(jobid).out
# @ output = {output_file}.$(jobid).out
# @ environment = COPY_ALL
# @ wall_clock_limit = {wall_time}
# @ notification = never
# @ job_type = bluegene
# @ bg_size = {nodes}
# @ bg_connectivity = {bg_connectivity}
# @ queue

cd {initial_dir}
