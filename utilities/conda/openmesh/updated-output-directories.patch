diff --git a/src/Python/CMakeLists.txt b/src/Python/CMakeLists.txt
index 099786d..3af948c 100644
--- a/src/Python/CMakeLists.txt
+++ b/src/Python/CMakeLists.txt
@@ -45,11 +45,11 @@ IF(OPENMESH_BUILD_PYTHON_BINDINGS)
 			MESSAGE(STATUS "Checking the Boost Python configuration")
 
 			SET(CMAKE_TRY_COMPILE_CONFIGURATION "Release")
-			
+
 			TRY_COMPILE(
 				COMPILE_WORKS
-				${CMAKE_CURRENT_BINARY_DIR}
-				${CMAKE_CURRENT_SOURCE_DIR}/Example/
+				${CMAKE_CURRENT_BINARY_DIR}/Example
+				${CMAKE_CURRENT_SOURCE_DIR}/Example
 				Example
 				CMAKE_FLAGS
 					"-DINCLUDE_DIRECTORIES:STRING=${PYTHON_INCLUDE_DIRS};${Boost_INCLUDE_DIRS}"
@@ -71,13 +71,9 @@ IF(OPENMESH_BUILD_PYTHON_BINDINGS)
 				FIND_PACKAGE(PythonInterp ${PYTHONLIBS_VERSION_STRING_STRIPPED} QUIET)
 
 				IF(PYTHONINTERP_FOUND)
-					
-					IF(MSVC)
-						SET(PYTHON_WORKING_DIR "${CMAKE_CURRENT_BINARY_DIR}/${CMAKE_TRY_COMPILE_CONFIGURATION}")
-					ELSE()
-						SET(PYTHON_WORKING_DIR "${CMAKE_CURRENT_BINARY_DIR}")
-					ENDIF()
-					
+
+					SET(PYTHON_WORKING_DIR "${CMAKE_CURRENT_BINARY_DIR}/Example/python/")
+
 					EXECUTE_PROCESS(
 						COMMAND ${PYTHON_EXECUTABLE} -c "from example import *; greet(); planet = World()"
 						WORKING_DIRECTORY ${PYTHON_WORKING_DIR}
@@ -97,9 +93,6 @@ IF(OPENMESH_BUILD_PYTHON_BINDINGS)
 							MESSAGE("Please consider updating Boost Python.")
 						ENDIF()
 
-						SET(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/Build/python/)
-						FILE(MAKE_DIRECTORY ${CMAKE_LIBRARY_OUTPUT_DIRECTORY})
-
 						FILE(GLOB SOURCES *.cc *hh)
 
 						INCLUDE_DIRECTORIES(${PYTHON_INCLUDE_DIRS} ${Boost_INCLUDE_DIRS} ../)
@@ -107,8 +100,6 @@ IF(OPENMESH_BUILD_PYTHON_BINDINGS)
 
 						ADD_LIBRARY(openmesh SHARED ${SOURCES})
 
-						install(TARGETS openmesh DESTINATION ${ACG_PROJECT_LIBDIR}/python )
-
 						TARGET_LINK_LIBRARIES(
 							openmesh
 							OpenMeshCore
@@ -125,6 +116,22 @@ IF(OPENMESH_BUILD_PYTHON_BINDINGS)
 							RELEASE_POSTFIX ""
 						)
 
+						SET(OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/Build/python)
+						FILE(MAKE_DIRECTORY ${OUTPUT_DIRECTORY})
+
+						SET_TARGET_PROPERTIES(openmesh PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${OUTPUT_DIRECTORY})
+						SET_TARGET_PROPERTIES(openmesh PROPERTIES LIBRARY_OUTPUT_DIRECTORY ${OUTPUT_DIRECTORY})
+						SET_TARGET_PROPERTIES(openmesh PROPERTIES ARCHIVE_OUTPUT_DIRECTORY ${OUTPUT_DIRECTORY})
+
+						FOREACH(CONFIG ${CMAKE_CONFIGURATION_TYPES})
+							STRING(TOUPPER ${CONFIG} CONFIG)
+							SET_TARGET_PROPERTIES(openmesh PROPERTIES RUNTIME_OUTPUT_DIRECTORY_${CONFIG} ${OUTPUT_DIRECTORY})
+							SET_TARGET_PROPERTIES(openmesh PROPERTIES LIBRARY_OUTPUT_DIRECTORY_${CONFIG} ${OUTPUT_DIRECTORY})
+							SET_TARGET_PROPERTIES(openmesh PROPERTIES ARCHIVE_OUTPUT_DIRECTORY_${CONFIG} ${OUTPUT_DIRECTORY})
+						ENDFOREACH()
+
+						INSTALL(TARGETS openmesh DESTINATION ${ACG_PROJECT_LIBDIR}/python)
+
 						IF(APPLE)
 							SET_TARGET_PROPERTIES(openmesh PROPERTIES SUFFIX ".so")
 							IF (NOT (CMAKE_MAJOR_VERSION LESS 3))
@@ -135,17 +142,6 @@ IF(OPENMESH_BUILD_PYTHON_BINDINGS)
 						IF(WIN32)
 							SET_TARGET_PROPERTIES(openmesh PROPERTIES SUFFIX ".pyd")
 							SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} /bigobj")
-
-							SET(OUTPUTS openmesh.exp openmesh.lib openmesh.pyd)
-
-							FOREACH(FILE ${OUTPUTS})
-								ADD_CUSTOM_COMMAND(
-									TARGET openmesh POST_BUILD
-									COMMAND ${CMAKE_COMMAND} -E copy
-										${CMAKE_CURRENT_BINARY_DIR}/${CMAKE_CFG_INTDIR}/${FILE}
-										${CMAKE_LIBRARY_OUTPUT_DIRECTORY}
-								)
-							ENDFOREACH()
 						ENDIF()
 
 						IF(OPENMESH_BUILD_PYTHON_UNIT_TESTS)
@@ -165,11 +161,12 @@ IF(OPENMESH_BUILD_PYTHON_BINDINGS)
 
 							# Copy library
 							IF(WIN32)
+								SET(OUTPUTS openmesh.exp openmesh.lib openmesh.pyd)
 								FOREACH(FILE ${OUTPUTS})
 									ADD_CUSTOM_COMMAND(
 										TARGET openmesh POST_BUILD
 										COMMAND ${CMAKE_COMMAND} -E copy
-											${CMAKE_CURRENT_BINARY_DIR}/${CMAKE_CFG_INTDIR}/${FILE}
+											${OUTPUT_DIRECTORY}/${FILE}
 											${UNITTEST_OUTPUT_DIRECTORY}
 									)
 								ENDFOREACH()
diff --git a/src/Python/Example/CMakeLists.txt b/src/Python/Example/CMakeLists.txt
index 03c278e..8200074 100644
--- a/src/Python/Example/CMakeLists.txt
+++ b/src/Python/Example/CMakeLists.txt
@@ -17,6 +17,20 @@ SET_TARGET_PROPERTIES(
 	RELEASE_POSTFIX ""
 )
 
+SET(OUTPUT_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/python/)
+FILE(MAKE_DIRECTORY ${OUTPUT_DIRECTORY})
+
+SET_TARGET_PROPERTIES(example PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${OUTPUT_DIRECTORY})
+SET_TARGET_PROPERTIES(example PROPERTIES LIBRARY_OUTPUT_DIRECTORY ${OUTPUT_DIRECTORY})
+SET_TARGET_PROPERTIES(example PROPERTIES ARCHIVE_OUTPUT_DIRECTORY ${OUTPUT_DIRECTORY})
+
+FOREACH(CONFIG ${CMAKE_CONFIGURATION_TYPES})
+	STRING(TOUPPER ${CONFIG} CONFIG)
+	SET_TARGET_PROPERTIES(example PROPERTIES RUNTIME_OUTPUT_DIRECTORY_${CONFIG} ${OUTPUT_DIRECTORY})
+	SET_TARGET_PROPERTIES(example PROPERTIES LIBRARY_OUTPUT_DIRECTORY_${CONFIG} ${OUTPUT_DIRECTORY})
+	SET_TARGET_PROPERTIES(example PROPERTIES ARCHIVE_OUTPUT_DIRECTORY_${CONFIG} ${OUTPUT_DIRECTORY})
+ENDFOREACH()
+
 IF(APPLE)
 	SET_TARGET_PROPERTIES(example PROPERTIES SUFFIX ".so")
 	IF (NOT (CMAKE_MAJOR_VERSION LESS 3))
